import styled from 'styled-components'
import * as colors from '../../resources/colors'

const Header = styled.div`

`
const ConfirmDate = styled.span`
  font-weight: bold;
  color: ${colors.primary};
`

export {Header, ConfirmDate}